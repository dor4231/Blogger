<<h1>List of All The Tags:</h1>

<ul>
    <% @tag.each do |tag| %>
        <li><%= link_to tag.name, tag_path(tag) %>
        <%= link_to "Delete", tag_path(tag), method: :delete, data: {confirm: "Really delete ALL tags?"} %></li>
    <% end %>
</ul>


<%= link_to  "<< Back to articles list", articles_path %>